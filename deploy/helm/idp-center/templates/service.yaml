apiVersion: v1
kind: Service
metadata:
  name: {{ include "idp-center.fullname" . }}
  labels:
    {{- include "idp-center.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "idp-center.selectorLabels" . | nindent 4 }}
